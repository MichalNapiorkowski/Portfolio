# ADC Sampling Test

Small helper sketch used while checking detector sampling behavior and timing.

Main file: `adc_sampling_test.ino`

The sketch prints sampling-related values over Serial and was used during early detector signal checks. It is included as a test artifact, not as the final detector firmware.
